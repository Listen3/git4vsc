# Git4VSC

Git4VSC 是一个使用本机 Git CLI 的 VS Code Git 增强扩展。

当前版本提供：

- 原生 Source Control 变更、暂存和提交体验
- 多仓库独立状态与操作队列
- Activity Bar 仓库列表
- 按拓扑顺序加载的 Commit Log
- 支持 merge 和 octopus merge 的提交图
- 原生 VS Code Diff 集成

安装后打开一个 Git 仓库，在 Activity Bar 中选择 Git4VSC，或从命令面板执行 `Git4VSC: Open Commit Log`。

